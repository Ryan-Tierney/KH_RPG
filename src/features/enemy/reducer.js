const initialState = { 
    position: [550, 540],
}

const enemyReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default enemyReducer